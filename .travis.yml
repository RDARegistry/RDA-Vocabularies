# safelist
branches:
  only:
  - gh-pages
  except:
  - master
  - gd-files
  - test-edit

language: ruby
before_install:
- travis_retry gem install jekyll
- travis_retry gem install kramdown
script: jekyll build --safe

